#include "main.h"

/**
 * _memcpy - function that copies memory area.
 *
 * @dest: destination to coppy to
 * @src: pointer to source to coppy from
 * @n: number of bytes from memory area @src
 *
 * Return: pointer @dest
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	int i = 0;

	while (--n)
	{
		dest = src[i++]
		dest++;
	}
	return (s);
}
